---
import { transformProps, UnpicImageProps } from "@unpic/core";
import { JSX } from "astro/astro";
interface Props
  extends UnpicImageProps<JSX.ImgHTMLAttributes<HTMLImageElement>> {}

const { ...rest } = Astro.props;
---

<img {...transformProps(rest as any)} />
